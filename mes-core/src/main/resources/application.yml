spring:
  application:
    name: mes-core
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/fabpilot?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:fabpilot}
    password: ${DB_PASSWORD:fabpilot_dev_password}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

server:
  port: ${SERVER_PORT:8080}

